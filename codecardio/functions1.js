// Create a function called sayHi that returns the string 'hi'
function sayHi() {
    return "hi";
}


// Make a function called sayHi that takes in a message parameter and then returns the message
function sayHi(message) {
    return message;
}

// Add a variable named total that is equal to num1 + num2
function addNums(num1, num2) {
    // code here 
        var total = num1 + num2;
    return total;
}

// Create a function called multiply that take two numbers as parameters. 
// Inside the function body create a variable called total that is equal to the two parameters 
// multiplied together, then return that variable
function multiply(a, b) {
    var total = a * b;
    return total;
}

// Return true if num1 is equal to num2
function isEqual(num1,num2) {
    if (num1 === num2) {
        return true;
    }
}

// Write a if statement that will check if num1 is equal to num2, if both numbers are equal then return true
function isEqual(num1,num2) {
    if (num1 === num2) {
        return true;
    }
}